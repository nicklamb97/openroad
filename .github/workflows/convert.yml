name: Convert Python to C# and Create PR

on:
  push:
    branches: [ main ]
    paths:
      - '**.py'  # This will only trigger on .py file changes

env:
  PYTHON_VERSION: '3.10'
  MODEL_NAME: 'NickLamb/Mistral-7B-Instruct-v0.3'

jobs:
  convert_and_pr:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Python repo
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
      
    - name: Get commit message
      run: |
        echo "📜 Retrieving commit message for SHA: ${{ github.sha }}"
        COMMIT_MESSAGE=$(git log --format=%B -n 1 "${{ github.sha }}")
        echo "COMMIT_MESSAGE<<EOF" >> $GITHUB_ENV
        echo "$COMMIT_MESSAGE" >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV
        echo "✅ Commit message retrieved and stored in environment variable"

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Cache Python dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Print content of changed file
      run: |
        echo "📄 Printing content of changed file(s):"
        git diff-tree --no-commit-id --name-only -r ${{ github.sha }} | grep '\.py$' | while read file; do
          echo "File: $file"
          echo "=========================="
          cat "$file"
          echo "=========================="
          echo ""
        done

    - name: Install dependencies
      run: |
        echo "🔧 Installing required Python packages..."
        python -m pip install --upgrade pip
        pip install instructlab gitpython requests
        echo "✅ Dependencies installed successfully"
        pip list
        echo "📦 Above is the list of installed packages"

    - name: Clone and copy config.yaml
      run: |
        echo "🔄 Cloning config repository and copying config.yaml..."
        git clone "https://github.com/nicklamb97/instructlab-config.git" temp_config
        cp temp_config/config.yaml "$GITHUB_WORKSPACE/config.yaml"
        rm -rf temp_config
        echo "✅ config.yaml copied to root directory: $GITHUB_WORKSPACE/config.yaml"

    - name: Download model
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
        echo "📥 Downloading Mistral-7B-Instruct-v0.3 model..."
        HF_TOKEN="$HF_TOKEN" ilab model download --repository=${{ env.MODEL_NAME }} --filename=Mistral-7B-Instruct-v0.3.Q4_K_M.gguf
        echo "✅ Model downloaded successfully"
        echo "MODEL_DOWNLOADED=true" >> $GITHUB_ENV

    - name: Start InstructLab server and convert Python to C#
      if: env.MODEL_DOWNLOADED == 'true'
      run: |
        mkfifo serverpipe
        ilab model serve > serverpipe 2>&1 &
        SERVER_PID=$!
        check_server_ready() {
          for i in {1..60}; do
            if curl -s http://localhost:8000/v1/models >/dev/null; then
              return 0
            fi
            sleep 5
          done
          return 1
        }

        if check_server_ready; then
          echo "Server is ready"
        else
          echo "Server failed to start within the timeout period"
          kill $SERVER_PID
          exit 1
        fi

        mkdir -p converted_files
        git diff-tree --no-commit-id --name-only -r ${{ github.sha }} | grep '\.py$' | while read file; do
          echo "Converting $file to C#"
          python_code=$(cat "$file")
          output_file="converted_files/$(basename "${file%.py}.cs")"
          
          csharp_code=$(curl -s -X POST http://localhost:8000/v1/chat/completions \
            -H "Content-Type: application/json" \
            -d '{
              "model": "Mistral-7B-Instruct-v0.3",
              "messages": [
                {"role": "system", "content": "You are a helpful assistant that converts Python code to C#."},
                {"role": "user", "content": "Convert the following Python code to C#:\n\n'"$python_code"'"}
              ]
            }' | jq -r '.choices[0].message.content')
                    
          echo "$csharp_code" > "$output_file"
          echo "Converted $file to $output_file"
        done

        kill $SERVER_PID
        wait $SERVER_PID

        rm serverpipe

    - name: Checkout C# repo
      uses: actions/checkout@v3
      with:
        repository: nicklamb97/csharp
        path: csharp_repo
        token: ${{ secrets.PAT_TOKEN }}

    - name: Copy converted files and create PR
      run: |
        cp -r converted_files/* csharp_repo/
        cd csharp_repo
        git config user.name "GitHub Actions Bot"
        git config user.email "<>"
        git add .
        git commit -m "Convert Python to C#: ${{ env.COMMIT_MESSAGE }}"
        git push origin HEAD:auto-convert-python-to-csharp
        
        PR_URL=$(curl -X POST -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
             -H "Accept: application/vnd.github.v3+json" \
             https://api.github.com/repos/nicklamb97/csharp/pulls \
             -d "{
               \"title\": \"Automated Python to C# Conversion\",
               \"body\": \"This PR contains automatically converted C# code based on the following commit:\\n\\n${{ env.COMMIT_MESSAGE }}\\n\\nPlease review the converted code and merge if appropriate.\",
               \"head\": \"auto-convert-python-to-csharp\",
               \"base\": \"main\"
             }" | jq -r .html_url)
        
        echo "Pull request created: $PR_URL"