name: Convert Python to C# and Create PR

on:
  push:
    branches: [ main ]
    paths:
      - '**.py'  # This will only trigger on .py file changes

env:
  PYTHON_VERSION: '3.10'
  MODEL_NAME: 'NickLamb/OpenROAD-8B-Instruct'

jobs:
  convert_and_pr:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Python repo
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
      
    - name: Get commit message
      run: |
        echo "📜 Retrieving commit message for SHA: ${{ github.sha }}"
        COMMIT_MESSAGE=$(git log --format=%B -n 1 "${{ github.sha }}")
        {
          echo "COMMIT_MESSAGE<<EOF"
          echo "$COMMIT_MESSAGE"
          echo "EOF"
        } >> "$GITHUB_ENV"
        echo "✅ Commit message retrieved and stored in environment variable"

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Cache Python dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Print content of changed file
      run: |
        echo "📄 Printing content of changed file(s):"
        git diff-tree --no-commit-id --name-only -r ${{ github.sha }} | grep '\.py$' | while read file; do
          echo "File: $file"
          echo "=========================="
          cat "$file"
          echo "=========================="
          echo ""
        done

    - name: Install dependencies
      run: |
        echo "🔧 Installing required Python packages..."
        python -m pip install --upgrade pip
        pip install instructlab gitpython
        echo "✅ Dependencies installed successfully"
        pip list
        echo "📦 Above is the list of installed packages"

    - name: Clone and copy config.yaml
      run: |
        echo "🔄 Cloning config repository and copying config.yaml..."
        git clone "https://github.com/nicklamb97/instructlab-config.git" temp_config
        cp temp_config/config.yaml "$GITHUB_WORKSPACE/config.yaml"
        rm -rf temp_config
        echo "✅ config.yaml copied to root directory: $GITHUB_WORKSPACE/config.yaml"

    - name: Download model
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
        echo "📥 Downloading OpenROAD-8B-Instruct model..."
        HF_TOKEN="$HF_TOKEN" ilab model download --repository=${{ env.MODEL_NAME }} --filename=OpenROAD-8B-Instruct.Q4_K_M.gguf
        echo "✅ Model downloaded successfully"
        echo "MODEL_DOWNLOADED=true" >> "$GITHUB_ENV"

    - name: Convert Python to C#
      if: env.MODEL_DOWNLOADED == 'true'
      run: |
        git diff-tree --no-commit-id --name-only -r ${{ github.sha }} | grep '\.py$' | while read file; do
          echo "Converting $file to C#"
          python_code=$(cat "$file")
          output_file="${file%.py}.cs"
          
          csharp_code=$(ilab chat -m OpenROAD-8B-Instruct --prompt "Convert the following Python code to C#:

        Python code:
        $python_code

        C# code:")
          
          echo "$csharp_code" > "$output_file"
          echo "Converted $file to $output_file"
        done

    - name: Create Pull Request
      uses: peter-evans/create-pull-request@v5
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        commit-message: "Convert Python to C#: ${{ env.COMMIT_MESSAGE }}"
        title: "Automated Python to C# Conversion"
        body: |
          This PR contains automatically converted C# code based on the following commit:

          ${{ env.COMMIT_MESSAGE }}

          Please review the converted code and merge if appropriate.
        branch: auto-convert-python-to-csharp
        base: main